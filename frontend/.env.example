# Optional. The app defaults to http://localhost:8001, which matches the backend,
# so you only need this file if you move either service to a different port.
# Copy to .env.local to use it.
NEXT_PUBLIC_API_BASE=http://localhost:8001
